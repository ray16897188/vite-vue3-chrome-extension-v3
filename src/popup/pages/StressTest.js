import Greeks from "./Greeks";

let priceChanges = [-0.15, -0.1, -0.05, 0, 0.05, 0.1, 0.15];

export default class StressTest {
    constructor(optionPositions, futurePositions, tickers, indexPrice) {

    }
}
